import { DefaultTheme } from "styled-components";

// theme 객체 특성들의 타입에 맞게 기본 color 설정
const theme: DefaultTheme = {
  bgColor: "#2f3640",
  listColor: "#f5f6fa",
  textColor: "#555d58",
  accentColor: "#9c88ff",
};

export { theme };
